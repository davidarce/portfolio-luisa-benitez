---
import { getCollection } from "astro:content";

import BaseLayout from "../../../layouts/BaseLayout.astro";
import Grid from "../../../components/Grid.astro";
import Card from "../../../components/Card.astro";
import Hero from "../../../components/Hero.astro";
import { sortProjects } from "../../../lib/sort-projects";
import { getTranslation } from "../../../i18n";
import { localeStaticPaths } from "../../../i18n/routing";

export const getStaticPaths = localeStaticPaths;

const { locale } = Astro.props;
const t = getTranslation(locale);

const editorials = sortProjects(await getCollection("editorials"));
---

<BaseLayout
    title={t.collections.editorials.title}
    description={t.collections.editorials.description}
>
    <div class="stack gap-20 lg:gap-48">
        <header class="wrapper">
        	<Hero title={t.collections.editorials.heading} align="start" />
        </header>
        <main id="contenido" class="wrapper stack gap-20 lg:gap-48">
            <Grid variant="small">
                {
                    editorials.map((editorial, i) => {
                        const { id, data } = editorial;
                        const {
                            title,
                            img,
                            img_alt,
                            cardSize,
                            aspectRatio,
                            objectPosition,
                        } = data;

                        return (
                            <li>
                                <Card
                                    id={id}
                                    page="editorials"
                                    title={title}
                                    img={img}
                                    img_alt={img_alt}
                                    cardSize={cardSize}
                                    aspectRatio={aspectRatio}
                                    objectPosition={objectPosition}
                                    priority={i === 0}
                                />
                            </li>
                        );
                    })
                }
            </Grid>
        </main>
    </div>
</BaseLayout>
