---
// Redirección estática: /work/ → home. La colección "work" (plantilla de
// Astro, lorem ipsum) se retira en la Fase 2 — ver
// plan/02-information-architecture/REVISION-v2.md §3 y
// .sdd/phase2-projects/plan.md T003-T004. El listado no tiene un equivalente
// 1:1: el trabajo real vive repartido en los 5 listados reales
// (editoriales, campañas, celebridades y eventos, runway, cine), así que el
// destino es la home. Mismo patrón de meta-refresh + canonical que
// /publicity/ y /celebrities/ — ver src/components/RedirectPage.astro.
import RedirectPage from "../../components/RedirectPage.astro";
---

<RedirectPage to="/" title="Trabajos | Luisa Benítez" />
