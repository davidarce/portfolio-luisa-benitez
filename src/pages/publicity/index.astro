---
// Redirección estática: /publicity → /campaigns (rename Fase 2 §4). Ver
// src/components/RedirectPage.astro para la limitación de GitHub Pages (sin
// 301 real) y por qué es meta-refresh + canonical en vez de la opción
// `redirects` de astro.config.mjs.
import RedirectPage from "../../components/RedirectPage.astro";
---

<RedirectPage to="/campaigns/" title="Publicidad ahora es Campañas | Luisa Benítez" />
